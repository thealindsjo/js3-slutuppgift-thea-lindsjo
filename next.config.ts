/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["upload.wikimedia.org", "flagcdn.com", "restcountries.com", "images.unsplash.com"],
  },
};

module.exports = nextConfig;